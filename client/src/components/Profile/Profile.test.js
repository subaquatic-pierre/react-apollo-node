import React from 'react';
import { mount } from 'enzyme';
import Profile from './Profile';

it('renders without error', () => {

})

it('shows all user info', () => {

})

it('redirects to home if no user logged in', () => {

})
