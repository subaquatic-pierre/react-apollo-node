import React from 'react';
import { render } from '../../../test/utils.js'
import Login from '../Login'

it('renders without error', () => {

})

it('logs a user in', () => {

})

it('redirects on user already logged in', () => {

})

