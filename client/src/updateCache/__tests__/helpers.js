import {
    removeRecipe,
    addRecipe,
    checkRecipeLiked,
    getFavourites
} from '../helpers';

const recipes = [
    {

    }
]

describe('remove recipe', () => {
    test('removes a recipe with given id', () => {

    })
    test('does not remove a recipe with given id', () => {

    })
})

describe('add recipe', () => {
    test('adds a recipe with given id', () => {

    })
    test('does not remove a recipe with given id', () => {

    })
})